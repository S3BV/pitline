---
const { href, title, description, className = "" } = Astro.props;
---

<a
  href={href}
  class={`group relative overflow-hidden snap-center rounded-2xl border border-black/10 bg-white p-6 shadow-soft transition hover:border-pitline/40 hover:shadow-lg focus:outline-none focus:ring-2 focus:ring-pitline/50 min-w-[260px] sm:min-w-[320px] ${className}`}
>
  <div class="absolute inset-0 bg-white transition-colors duration-300 group-hover:bg-pitline group-focus-within:bg-pitline"></div>
  <div class="relative z-10">
    <h3 class="text-xl font-bold uppercase text-pitline transition-colors duration-300 group-hover:text-white group-focus-within:text-white">
      {title}
    </h3>
    <p class="mt-2 text-sm text-slate transition-colors duration-300 group-hover:text-white/90 group-focus-within:text-white/90">
      {description}
    </p>
  </div>
</a>
