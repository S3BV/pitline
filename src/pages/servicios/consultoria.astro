---
import BaseLayout from "../../layouts/BaseLayout.astro";
import AnimatedServiceCard from "../../components/AnimatedServiceCard.astro";

const title = "Consultoría | Pitline";
const description =
  "Consultoría especializada para optimizar procesos y rendimiento automotriz con enfoque en eficiencia y control.";
---

<BaseLayout title={title} description={description}>
  <div class="dev-page">
    <header class="fixed top-0 z-50 w-full border-b border-black/10 bg-white/70 backdrop-blur">
      <div class="container-xl flex items-center justify-between py-2">
        <a href="/" class="inline-flex items-center">
          <img src="/imagenes/pitlinelogo2-clean-235.webp" alt="Pitline" class="h-16 w-auto" />
        </a>
        <a
          href="/#contacto"
          class="inline-flex items-center gap-2 rounded-full bg-pitline px-7 py-3 text-sm font-semibold uppercase tracking-wide text-white shadow-soft transition hover:bg-pitline-dark"
        >
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <path
              d="M6 8h12M6 12h7m-9 8 3-3H18a4 4 0 0 0 4-4V7a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v7a4 4 0 0 0 4 4z"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            />
          </svg>
          Hablemos
        </a>
      </div>
    </header>

    <main class="pt-28">
      <section class="py-20">
        <div class="container-xl">
          <div class="rounded-3xl border border-white/15 bg-white/40 p-8 shadow-soft backdrop-blur md:p-10">
            <div>
              <p class="text-xs font-semibold uppercase tracking-[0.2em] text-slate">Servicios</p>
              <div class="mt-4 flex flex-wrap items-center gap-4">
                <h1 class="text-4xl font-black uppercase tracking-wide text-pitline">Consultoría</h1>
              </div>
              <p class="mt-6 max-w-2xl text-base text-slate">
                Asesoría especializada para optimizar sistemas y procesos, reducir tiempos operativos y elevar el
                rendimiento con decisiones basadas en datos.
              </p>

              <div class="mt-10 grid gap-6 md:grid-cols-2">
                <div class="rounded-2xl border border-white/40 bg-white/80 p-3 shadow-soft backdrop-blur">
                  <img
                    src="/imagenes/shakeands.webp"
                    alt=""
                    class="h-44 w-full rounded-xl object-cover"
                    loading="lazy"
                    decoding="async"
                    aria-hidden="true"
                  />
                  <h2 class="mt-4 text-lg font-bold uppercase text-pitline">Factibilidad Técnica</h2>
                  <p class="mt-2 text-xs font-semibold uppercase tracking-[0.2em] text-slate">
                    Evaluación para nuevos desarrollos
                  </p>
                  <p class="mt-3 text-sm text-slate">
                    Acordamos la arquitectura y el stack tecnológico óptimo, mitigando riesgos críticos y asegurando
                    que tu idea sea ejecutable antes de comprometer recursos en la fase de construcción.
                  </p>
                </div>
                <div class="rounded-2xl border border-white/40 bg-white/80 p-3 shadow-soft backdrop-blur">
                  <img
                    src="/imagenes/casa.webp"
                    alt=""
                    class="h-44 w-full rounded-xl object-cover"
                    loading="lazy"
                    decoding="async"
                    aria-hidden="true"
                  />
                  <h2 class="mt-4 text-lg font-bold uppercase text-pitline">Plan de Modernización</h2>
                  <p class="mt-2 text-xs font-semibold uppercase tracking-[0.2em] text-slate">
                    Evolución y escalabilidad de sistemas
                  </p>
                  <p class="mt-3 text-sm text-slate">
                    Definimos pasos concretos para mejorar tu rendimiento hoy y asegurar que tu plataforma crezca al mismo ritmo que tus metas, sin perder eficiencia en el camino.
                  </p>
                </div>
              </div>
            </div>

            <section class="mt-16">
              <div class="flex items-center justify-between">
                <h2 class="text-lg font-bold uppercase text-pitline">Explora otros servicios</h2>
              </div>

              <div class="mt-6 overflow-x-auto pb-4">
                <div class="flex w-max gap-6 snap-x snap-mandatory">
                  <AnimatedServiceCard
                    href="/servicios/desarrollo-software"
                    title="Desarrollo de Software"
                    description="Soluciones a medida para automatizar y escalar tu operación."
                  />
                  <AnimatedServiceCard
                    href="/servicios/branding"
                    title="Branding"
                    description="Identidad visual sólida para una marca de alto impacto."
                  />
                </div>
              </div>
            </section>
          </div>
        </div>
      </section>
    </main>
  </div>
</BaseLayout>

<style is:global>
  .dev-page {
    position: relative;
    min-height: 100vh;
  }

  .dev-page::before {
    content: "";
    position: fixed;
    inset: 0;
    background-image: linear-gradient(rgba(255, 255, 255, 0.45), rgba(255, 255, 255, 0.45)),
      url("/imagenes/zonaverde.webp");
    background-size: cover;
    background-position: center;
    z-index: -1;
  }

  @media (max-width: 768px) {
    .dev-page::before {
      position: absolute;
      background-size: 100% auto;
      background-position: top center;
    }
  }
</style>
